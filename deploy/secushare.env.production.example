# SecuShare production environment file
# Copy to: /etc/secushare/secushare.env
# Permissions: chmod 600 /etc/secushare/secushare.env

# Core runtime
ENVIRONMENT=production
SERVER_BIND_ADDRESS=127.0.0.1
SERVER_PORT=8080
DATABASE_PATH=/opt/secushare/data/secushare.db
STORAGE_PATH=/opt/secushare/storage/files

# Security / auth
# Generate with: openssl rand -hex 32
JWT_SECRET=replace-with-64-hex-or-random-32+-char-secret

# Required in production. Generate once and keep stable across restarts.
# This is a base64-encoded JSON value produced by your OPAQUE setup tooling.
OPAQUE_SERVER_SETUP=replace-with-generated-opaque-server-setup

# Guest session duration in hours
GUEST_DURATION_HOURS=24

# Must match your single-origin frontend host (or a comma-separated allowlist)
ALLOW_ORIGINS=https://your-domain.example

# Reverse proxy trust boundary for client IP extraction.
# For same-host nginx -> backend deployments, keep the loopback defaults.
TRUSTED_PROXIES=127.0.0.1,::1

# Email verification (required in production)
SMTP_HOST=smtp.your-provider.example
SMTP_PORT=587
SMTP_FROM=no-reply@your-domain.example
SMTP_USERNAME=optional-username
SMTP_PASSWORD=optional-password

# Observability
# Recommended strict default: disabled
METRICS_ENABLED=false
# Required only when METRICS_ENABLED=true (send as Bearer token)
METRICS_TOKEN=

# Optional logging tuning
LOG_LEVEL=info
LOG_FORMAT=json
