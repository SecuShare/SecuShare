# SecuShare Frontend Configuration
# Copy this file to .env and modify as needed

# Backend API URL
# In development, use a relative path so Vite proxies requests to the backend
# (avoids cross-origin cookie issues with CSRF protection).
# The Vite dev server proxies /api -> http://localhost:8080.
VITE_API_URL=/api/v1

# In production, set this to the absolute backend URL, e.g.:
# VITE_API_URL=https://api.yourdomain.com/api/v1
