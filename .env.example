# Server Configuration
SERVER_BIND_ADDRESS=127.0.0.1
SERVER_PORT=8080

# Environment (development or production)
ENVIRONMENT=development

# Database
DATABASE_PATH=./data/secushare.db

# Storage
STORAGE_PATH=./storage

# Authentication - REQUIRED: Generate a strong random secret (min 32 chars)
# Example: openssl rand -hex 32
JWT_SECRET=
# Dedicated HMAC key for share download verification codes.
# Required in production and must be different from JWT_SECRET.
DOWNLOAD_CODE_HMAC_SECRET=
# Required in production. Optional in development (auto-generated if omitted).
OPAQUE_SERVER_SETUP=
GUEST_DURATION_HOURS=24

# CORS - REQUIRED: Set to your frontend domain(s), comma-separated
# Example: https://secushare.example.com
ALLOW_ORIGINS=

# Reverse proxy trust boundary for client IP extraction (comma-separated IP/CIDR)
# Keep this limited to your actual proxy hops only.
# The docker-compose default includes 172.16.0.0/12 for bridge networks.
TRUSTED_PROXIES=127.0.0.1,::1,172.16.0.0/12

# Email verification (required in production)
SMTP_HOST=
SMTP_PORT=587
SMTP_FROM=no-reply@secushare.local
SMTP_USERNAME=
SMTP_PASSWORD=

# Observability
# In production, metrics are disabled by default. If enabled in production,
# METRICS_TOKEN is required and must be sent as: Authorization: Bearer <token>
METRICS_ENABLED=false
METRICS_TOKEN=

# Frontend build-time API URL (use /api/v1 for same-origin reverse proxy deployments)
VITE_API_URL=/api/v1

# Docker Ports (used by docker-compose)
# You can set BACKEND_PORT=127.0.0.1:8080 to keep the API private on the host.
BACKEND_PORT=8080
FRONTEND_PORT=3000
